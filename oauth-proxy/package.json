{
  "name": "weiying-portfolio-oauth-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that exchanges GitHub OAuth codes for access tokens on behalf of the static admin UI.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.95.0"
  }
}
