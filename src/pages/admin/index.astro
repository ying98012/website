---
import BaseLayout from "@/layouts/BaseLayout.astro";

const base = import.meta.env.BASE_URL;
const pagesCmsUrl = "https://pagescms.org";
const repoUrl = "https://github.com/ying98012/website";
---

<BaseLayout title="Pages CMS 管理入口" disableMainWrapper={true}>
  <div class="grid min-h-screen place-items-center px-6 py-12">
    <section class="glass-panel w-full max-w-2xl space-y-5 rounded-xl p-6">
      <header class="space-y-2">
        <h1 class="text-2xl font-bold">Pages CMS 管理入口</h1>
        <p class="text-sm text-on-surface-variant">
          目前網站內容採用 Pages CMS。請從下方連結登入 GitHub 後，選擇本 repo 即可編輯 `src/content/**` 內容。
        </p>
      </header>

      <div class="rounded-lg border border-outline-variant/30 bg-surface-container-low p-5 text-sm text-on-surface-variant">
        <ol class="list-decimal space-y-2 pl-5">
          <li>先確認 GitHub 已安裝 Pages CMS App，且對本 repo 有寫入權限。</li>
          <li>進入 Pages CMS，選擇 <code>ying98012/website</code>。</li>
          <li>在 UI 修改內容並儲存後，變更會直接提交到 GitHub。</li>
        </ol>
      </div>

      <div class="flex flex-wrap gap-3 pt-2">
        <a
          class="btn-secondary rounded px-5 py-3 font-label-caps text-label-caps uppercase tracking-widest"
          href={pagesCmsUrl}
          target="_blank"
          rel="noreferrer"
        >
          開啟 Pages CMS
        </a>
        <a
          class="btn-secondary rounded px-5 py-3 font-label-caps text-label-caps uppercase tracking-widest"
          href={repoUrl}
          target="_blank"
          rel="noreferrer"
        >
          開啟 GitHub Repo
        </a>
      </div>

      <div class="rounded-lg border border-outline-variant/30 bg-surface-container-low p-4 text-xs text-on-surface-variant">
        專案設定檔位於根目錄 <code>.pages.yml</code>，並提供 <code>admin/config.yml</code> 供相容流程使用。
      </div>

      <a class="block text-center text-sm text-secondary underline underline-offset-4" href={base}>回到首頁</a>
    </section>
  </div>
</BaseLayout>
